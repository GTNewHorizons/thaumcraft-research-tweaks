package elan.tweaks.thaumcraft.research.frontend.integration.table.gui.layout

import elan.tweaks.common.gui.dto.Vector2D

object AspectTooltipLayout {
  val textBoxOffset = Vector2D(12, -20)

  val firstTagBackgroundOffset = Vector2D(6, 6)
  val firstTagOffset = firstTagBackgroundOffset + Vector2D(2, 2)

  val secondTagBackgroundOffset = Vector2D(24, 6)
  val secondTagOffset = secondTagBackgroundOffset + Vector2D(2, 2)
}
